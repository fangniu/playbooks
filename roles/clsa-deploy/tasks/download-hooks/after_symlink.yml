- name: ANSISTRANO | download ansistrano_after_symlink_tasks_file
  get_url:
    url: "{{ ansistrano_after_symlink_tasks_file_url }}"
    dest: "{{ ansistrano_after_symlink_tasks_file }}"
    force: yes
    headers: "{{ ansistrano_cmdb_download_headers | default(omit) }}"
